# Decisions

- API keys must never be placed in frontend code.
- OpenAI must be called only from the Flask backend.
- Keep the interface simple and iPhone-friendly.
- Keep Render compatibility with `gunicorn app:app`.

